# TTS STUDIO — FROM ZERO PRODUCTION BUILD
# WINDOWS DESKTOP APPLICATION
# EXISTING TTS LOGIC + EXISTING ZERO-SHOT VOICES + DYNAMIC MODEL RUNTIMES

START THIS PROJECT FROM ZERO.

IMPORTANT:

I have created a completely new clean project and a new .venv.

I have restored my ORIGINAL TTS logic:

scripts/
├── tts_adapters.py
└── speech_synth_helper.py

I have also restored my ORIGINAL zero-shot reference voices:

voices/

These existing Python files and WAV files are the SOURCE OF TRUTH.

Do not replace them with newly invented logic.

Do not recreate the old pipeline from memory.

Read and trace the actual files first.

PHASE 0.1 — CREATE CLEAN DEVELOPMENT ENVIRONMENT

Before installing any project dependencies:

1. Verify the installed Python versions on Windows.

2. Select the Python version compatible with the existing
   TTS logic and supported model runtimes.

3. Create a project-local virtual environment:

   .venv

4. Activate the new .venv.

5. Upgrade only the basic packaging tools:

   pip
   setuptools
   wheel

6. DO NOT install all model dependencies yet.

7. First inspect:
   - scripts/tts_adapters.py
   - scripts/speech_synth_helper.py
   - requirements.txt
   - model-specific requirements
   - existing imports

8. Generate a dependency matrix before installing large
   packages such as PyTorch.

9. Then install the minimum dependencies required by the
   currently implemented baseline.

10. Verify:

    python --version
    pip --version
    python -c "import torch; print(torch.__version__)"
    python -c "import torch; print(torch.cuda.is_available())"

11. Record the exact environment in:

    docs/INITIAL_RUNTIME_AUDIT.md

IMPORTANT:

This .venv is ONLY the development environment.

It must NOT be copied wholesale into the final installer.

The production application will later package the required
runtime components using the approved packaging architecture.

Do NOT create isolated model environments until the dependency
audit proves they are necessary.

============================================================
1. MAIN PROJECT GOAL
============================================================

Build a production-level Windows desktop application called:

TTS Studio

The final architecture is:

MY EXISTING TTS LOGIC
+
MY ORIGINAL ZERO-SHOT VOICES
+
PRODUCTION DESKTOP UI
+
LOCAL BACKEND
+
MODEL MANAGER
+
OFFICIAL MODEL DOWNLOADER
+
GPU/CPU DETECTION
+
PACKAGED RUNTIME

The final user should install:

TTS Studio.exe

The user should NOT need to manually install:

- Python
- pip
- virtualenv
- Node.js
- npm
- VS Code
- Git
- CUDA Toolkit
- cuDNN
- PyTorch manually

The application should manage the required runtime internally.

============================================================
2. ABSOLUTE SOURCE-OF-TRUTH RULE
============================================================

The following files are authoritative:

scripts/tts_adapters.py

scripts/speech_synth_helper.py

They contain my existing TTS behavior.

Preserve:

- preprocessing
- pronunciation processing
- chunking
- voice selection
- reference audio handling
- speaker conditioning
- model invocation
- audio processing
- output generation
- existing parameters
- existing voice logic

Do not rewrite them unnecessarily.

Do not replace them with a new TTS pipeline.

If integration requires an abstraction layer, wrap the existing logic.

Do not change generation behavior merely to make packaging easier.

============================================================
3. ORIGINAL VOICE ASSETS
============================================================

The following directory contains my original zero-shot reference voices:

voices/

Use the ORIGINAL WAV files.

Do NOT:

- rename
- resample
- normalize
- recompress
- clean
- alter
- replace
- regenerate
- change sample rate
- change channels

The packaged application must use these exact voice assets.

The voice categories must remain available.

The voice selection in the desktop application must map to the same reference WAV files used by the original pipeline.

============================================================
4. FIRST PHASE — COMPLETE AUDIT
============================================================

DO NOT BUILD THE UI FIRST.

First inspect:

scripts/tts_adapters.py
scripts/speech_synth_helper.py
voices/
configs/
requirements.txt
all model-related files
all imports
all model libraries
all runtime dependencies

Trace:

TEXT
 ↓
PREPROCESSING
 ↓
PRONUNCIATION
 ↓
CHUNKING
 ↓
VOICE SELECTION
 ↓
REFERENCE WAV
 ↓
CONDITIONING
 ↓
MODEL
 ↓
MODEL RUNTIME
 ↓
INFERENCE
 ↓
AUDIO PROCESSING
 ↓
FINAL WAV

Document the exact pipeline.

Create:

docs/EXISTING_PIPELINE.md

============================================================
5. MODELS
============================================================

Target models:

1. F5-TTS
2. Chatterbox Turbo
3. Fish Speech S2
4. OmniVoice
5. CosyVoice 3
6. XTTS-v2
7. IndexTTS2

IMPORTANT:

Do not assume that a model folder means that the model is implemented.

For every model determine:

- official library
- official repository
- model ID
- model class
- Python requirement
- PyTorch requirement
- torchaudio requirement
- transformers requirement
- other dependencies
- CUDA requirement
- CPU support
- Windows support
- compiler requirements
- model weight size
- runtime size
- download source
- local storage location
- whether isolated environment is required

Create:

docs/MODEL_RUNTIME_MATRIX.md

============================================================
6. REAL MODEL RULE
============================================================

A model is READY only if:

official library
+
required dependencies
+
weights
+
actual model class
+
actual inference
+
reference voice
+
real WAV output

all work.

Do not mark a model READY simply because:

- pip installed
- model files exist
- import works
- UI card exists

A real inference test is required.

============================================================
7. NO SILENT FALLBACK
============================================================

HARD RULE:

SELECTED MODEL = ACTUAL MODEL EXECUTED

If user selects:

Chatterbox

the application must execute:

Chatterbox.

It must NEVER silently execute:

F5-TTS.

If a model cannot run:

return:

DEPENDENCY_MISSING

or:

MODEL_NOT_DOWNLOADED

or:

MODEL_LOAD_FAILED

or:

INFERENCE_FAILED

Never hide the failure.

============================================================
8. PYTHON RUNTIME
============================================================

The development environment currently uses a fresh .venv.

Inspect the actual source code and determine the required Python version.

Do NOT assume the latest Python version is appropriate.

Prefer a stable Python version compatible with all supported runtimes.

The production user must NOT need Python installed globally.

The required Python runtime must be packaged with the backend using the selected packaging strategy.

============================================================
9. PYTORCH
============================================================

PyTorch is a major runtime dependency.

Determine the exact PyTorch version required by each model.

Do NOT blindly force all seven models into one PyTorch version.

If model requirements conflict:

USE ISOLATED MODEL RUNTIMES.

For example:

Main Runtime
    ├── F5-TTS
    └── Chatterbox

Isolated Runtime
    ├── XTTS-v2
    └── other incompatible model

The main runtime must not be broken just to support another model.

Create:

docs/PYTORCH_RUNTIME_MATRIX.md

Include:

Model
Python
PyTorch
TorchAudio
Transformers
CUDA Runtime
CPU Support
GPU Support
Isolation Required

============================================================
10. CUDA / GPU ARCHITECTURE
============================================================

The application must support:

GPU users

AND

CPU-only users.

Do NOT require users to manually install the CUDA Toolkit.

The packaged application should use the CUDA-enabled PyTorch/runtime when compatible with the user's NVIDIA GPU and driver.

At application startup detect:

- NVIDIA GPU present?
- GPU name
- VRAM
- NVIDIA driver version
- CUDA compatibility
- PyTorch CUDA availability
- available VRAM
- CPU
- system RAM

Create a hardware capability report.

Example:

GPU detected:
NVIDIA GeForce RTX ...

VRAM:
6 GB

CUDA:
AVAILABLE

Recommended device:
CUDA

OR:

GPU not detected.

Recommended device:
CPU

============================================================
11. GPU DEVICE SELECTION
============================================================

Runtime decision:

IF compatible NVIDIA GPU exists:

    use CUDA

ELSE:

    use CPU

Never crash because CUDA is unavailable.

Never pretend CPU inference is GPU inference.

Expose the selected device in Diagnostics.

Example:

DEVICE:
CUDA

or:

DEVICE:
CPU

============================================================
12. NVIDIA DRIVER
============================================================

The application must inspect the installed NVIDIA driver.

Do not assume a specific driver version.

Determine compatibility between:

NVIDIA Driver
+
PyTorch CUDA runtime
+
selected model.

If the driver is too old:

show:

NVIDIA DRIVER UPDATE REQUIRED

Do not silently fail.

Do not force-install GPU drivers.

Provide a clear diagnostic message.

============================================================
13. CUDA TOOLKIT
============================================================

The end user should NOT need the full CUDA Toolkit installed just to run the packaged application.

Do not require:

nvcc

unless a particular model genuinely requires compilation.

Prefer packaged/prebuilt runtime libraries.

Detect whether a model has a special compiler requirement.

============================================================
14. CPU FALLBACK
============================================================

Every model must declare:

GPU_SUPPORTED
CPU_SUPPORTED

If CPU inference is supported:

allow:

DEVICE = CPU

If the model is GPU-only:

show:

GPU_REQUIRED

Do not fake CPU support.

The UI must accurately report this.

============================================================
15. MODEL DOWNLOAD SYSTEM
============================================================

Do NOT bundle all massive model weights into TTS Studio.exe.

The installer should contain:

- application
- UI
- backend
- existing TTS logic
- existing voice assets
- configurations
- model manager
- runtime manager
- diagnostics

Large model weights should be downloaded when the user requests them.

Official sources only.

Possible sources:

- Hugging Face
- ModelScope
- official GitHub releases

Never use random third-party model mirrors unless explicitly approved.

============================================================
16. MODEL STORAGE
============================================================

Production model data should NOT depend on the developer project directory.

Use:

%LOCALAPPDATA%\TTS-Studio\models\

Structure:

%LOCALAPPDATA%\TTS-Studio\
│
├── models\
│   ├── f5tts\
│   ├── chatterbox\
│   ├── fishspeech\
│   ├── omnivoice\
│   ├── cosyvoice\
│   ├── xttsv2\
│   └── indextts2\
│
├── cache\
├── outputs\
├── logs\
├── config\
└── runtimes\

============================================================
17. MODEL DOWNLOAD FLOW
============================================================

User opens TTS Studio.

Model Manager checks:

Is model installed?

IF NO:

    show DOWNLOAD

IF YES:

    verify installation.

Lifecycle:

NOT_INSTALLED
 ↓
DOWNLOAD
 ↓
VERIFY
 ↓
INSTALL
 ↓
LOAD
 ↓
SMOKE TEST
 ↓
READY

Failure:

DOWNLOAD_FAILED
CHECKSUM_FAILED
DEPENDENCY_MISSING
MODEL_LOAD_FAILED
INFERENCE_FAILED
PLATFORM_UNSUPPORTED

============================================================
18. RESUMABLE DOWNLOADS
============================================================

Large models can be several GB.

The downloader must support:

- progress
- pause if possible
- resume
- retry
- network failure handling
- disk-space checking
- partial download cleanup
- integrity verification

Do not restart a 5 GB download unnecessarily after a temporary network failure.

============================================================
19. MODEL VERIFICATION
============================================================

After downloading:

verify required files.

If official checksums are available:

verify checksums.

Then:

load model

and run a small real inference test.

Only then:

STATUS = READY

============================================================
20. MODEL VERSION MANAGEMENT
============================================================

Do not use:

latest

without tracking a specific model version/revision where possible.

Create a model manifest.

Example:

model_manifest.json

Containing:

- model ID
- repository
- revision/version
- download files
- required dependencies
- runtime
- expected size
- verification data
- device support

============================================================
21. UI STACK
============================================================

Use exactly:

Electron
Vite
React
TypeScript
Tailwind CSS
shadcn/ui
Framer Motion
Zustand
React Query
electron-builder

Do not introduce unnecessary frontend frameworks.

============================================================
22. UI PAGES
============================================================

Build:

Dashboard
Models
Voices
Generate
History
Benchmark
Diagnostics
Settings
Downloads
About

============================================================
23. MODEL MANAGER UI
============================================================

Each model card should show:

Model name
Version
Status
Installed size
Download size
GPU support
CPU support
Runtime
Download button
Verify button
Test button
Delete model button

Example:

F5-TTS

READY ✓

Device:
CUDA

Weights:
1.28 GB

Runtime:
Available

============================================================
24. VOICE MANAGER
============================================================

Show all original voices.

Example:

Announcement
Audiobook
Narration
Podcast
Presentation
Social Media
Storytelling

Selecting a voice must select the exact original WAV file.

Do not create another voice representation unless the existing pipeline requires it.

============================================================
25. GENERATION PAGE
============================================================

User selects:

MODEL

VOICE

TEXT

SETTINGS

Then:

GENERATE

The request must go:

React
 ↓
Backend
 ↓
Runtime Manager
 ↓
Existing TTS pipeline
 ↓
Existing adapters
 ↓
Actual selected model
 ↓
Original reference WAV
 ↓
WAV output

============================================================
26. RUNTIME MANAGER
============================================================

Create:

runtime_manager.py

Its responsibility:

- detect runtime
- select model runtime
- select GPU/CPU
- start isolated runtime when required
- manage model lifecycle
- report errors
- report progress
- prevent incompatible runtimes from interfering with each other

It must NOT contain duplicated TTS generation logic.

============================================================
27. MODEL ISOLATION
============================================================

If dependencies conflict:

do not modify the main environment.

Use:

runtimes/
├── main/
├── xtts/
├── cosyvoice/
├── omnivoice/
├── fishspeech/
└── indextts/

ONLY create isolated runtimes where actually necessary.

Do not create seven environments automatically.

Measure first.

============================================================
28. PRODUCTION PYTHON
============================================================

The user must not see:

.venv

Python installation steps.

PyInstaller or an equivalent reliable packaging mechanism should package the required Python runtime/application components.

Do NOT copy the developer .venv wholesale.

Do NOT copy pip cache.

Do NOT copy temporary files.

Do NOT copy unrelated Hugging Face cache files.

============================================================
29. PRODUCTION PATHS
============================================================

Never hard-code:

D:\S-Project\TTS

inside production code.

Development:

D:\...\TTS

Production:

%LOCALAPPDATA%\TTS-Studio\

Use dynamic resource resolution.

============================================================
30. AUDIO QUALITY
============================================================

Do not compromise voice quality.

Do NOT change:

reference WAV
conditioning
chunking
preprocessing
model parameters
sampling logic

just to improve:

startup speed
installer size
VRAM usage
CPU usage

Optimization must target infrastructure.

============================================================
31. HARDWARE ADAPTATION
============================================================

The application should automatically choose:

NVIDIA GPU
or
CPU

based on actual compatibility.

Example:

GPU detected + compatible:
CUDA

GPU detected but incompatible:
CPU + warning

No GPU:
CPU

Do not make the user configure PyTorch manually.

============================================================
32. VRAM MANAGEMENT
============================================================

Do not load all massive models simultaneously.

Use:

LOAD
 ↓
INFERENCE
 ↓
UNLOAD

when appropriate.

Show:

VRAM used
VRAM available
model loading status

If insufficient VRAM:

show:

INSUFFICIENT VRAM

instead of crashing.

============================================================
33. SYSTEM REQUIREMENTS
============================================================

Create an automatic system checker.

Check:

Windows version
CPU
RAM
GPU
VRAM
NVIDIA driver
CUDA compatibility
disk space
runtime availability
model availability

Create:

docs/SYSTEM_REQUIREMENTS.md

Do not guess requirements.

Derive model-specific requirements from the actual model libraries and inference tests.

============================================================
34. CLEAN MACHINE TEST
============================================================

The final application must be tested on a clean Windows machine.

The clean machine should NOT have:

Python
Node
npm
Git
developer .venv
project source

installed.

Install:

TTS Studio.exe

Then verify:

application starts
hardware detection works
model manager works
model download works
model verification works
voice manager works
original voice loads
generation works
GPU works when available
CPU works when GPU unavailable
outputs are saved
application can restart
already downloaded models are reused

============================================================
35. OFFLINE MODE
============================================================

After the required model is downloaded:

normal inference should work without Internet whenever the model itself supports offline operation.

The UI should show:

OFFLINE READY

when all required files are locally available.

Internet should only be required for:

model download
updates
optional online functionality

============================================================
36. IMPORTANT — MODEL DEPENDENCIES
============================================================

Do not assume one global requirements.txt is enough.

Create:

requirements/
├── core.txt
├── f5tts.txt
├── chatterbox.txt
├── fishspeech.txt
├── omnivoice.txt
├── cosyvoice.txt
├── xttsv2.txt
└── indextts2.txt

Only after auditing the actual model requirements.

Do not blindly install everything into one environment.

============================================================
37. DEVELOPMENT DEPENDENCY BASELINE
============================================================

The previous project successfully used a baseline around:

Python 3.10
PyTorch 2.4.1
CUDA 12.1

but this is NOT automatically the requirement for every model.

Use this as a starting reference only.

Verify every model individually.

Do not upgrade/downgrade the main runtime merely to satisfy an incompatible model.

============================================================
38. TEST MATRIX
============================================================

For every model test:

1. Import
2. Dependency check
3. Weight check
4. Model initialization
5. Device detection
6. Reference voice loading
7. Real inference
8. WAV generation
9. Audio validation
10. Runtime cleanup

Record:

model
runtime
python
torch
torchaudio
transformers
device
VRAM
inference time
audio duration
RTF
status

============================================================
39. GOLDEN REGRESSION TEST
============================================================

The old TTS pipeline and the new packaged backend must be compared.

Use:

same text
same model
same original reference WAV
same preprocessing
same pronunciation
same chunking
same conditioning
same generation settings

Verify:

PIPELINE EQUIVALENCE
VOICE ASSET EQUIVALENCE
MODEL IDENTITY
PREPROCESSING EQUIVALENCE
CHUNKING EQUIVALENCE
CONDITIONING EQUIVALENCE
AUDIO FORMAT
NO SILENT FALLBACK

============================================================
40. PACKAGING ARCHITECTURE
============================================================

Final architecture:

TTS Studio.exe
       │
       ├── Electron
       ├── React UI
       ├── FastAPI/backend
       ├── Existing TTS logic
       ├── Original voice assets
       ├── Runtime Manager
       ├── Model Manager
       └── Diagnostics
               │
               ▼
       Local Runtime Manager
               │
       ├── Main Runtime
       ├── Isolated Runtime A
       ├── Isolated Runtime B
       └── ...
               │
               ▼
       %LOCALAPPDATA%\TTS-Studio\models
               │
               ▼
       Official Model Weights

============================================================
41. INSTALLER
============================================================

Use:

electron-builder

for the Windows application.

Use PyInstaller or another appropriate backend packaging mechanism
for the Python backend.

The final installer should NOT contain all massive model weights.

The Model Manager downloads those separately.

============================================================
42. UNINSTALLATION
============================================================

The user should be able to uninstall the application.

Model data should be treated separately from application binaries.

Prefer:

Application uninstall
≠
Automatic deletion of multi-GB model data

Ask the user whether they want to remove downloaded models.

Never silently delete user model data.

============================================================
43. DOCUMENTATION
============================================================

Create:

docs/
├── ARCHITECTURE.md
├── EXISTING_PIPELINE.md
├── MODEL_RUNTIME_MATRIX.md
├── DEPENDENCY_MATRIX.md
├── SYSTEM_REQUIREMENTS.md
├── GPU_CPU_ARCHITECTURE.md
├── MODEL_DOWNLOAD_ARCHITECTURE.md
├── PACKAGING.md
├── VOICE_ASSET_INVENTORY.md
└── TESTING.md

============================================================
44. EXECUTION RULE
============================================================

DO NOT immediately start coding everything.

FIRST:

AUDIT.

Then:

ARCHITECTURE.

Then:

BACKEND.

Then:

MODEL MANAGER.

Then:

VOICE MANAGER.

Then:

GENERATION.

Then:

UI.

Then:

TESTING.

Then:

PACKAGING.

============================================================
45. FIRST TASK — ONLY THIS
============================================================

Start now with:

PHASE 0 — CLEAN PROJECT AUDIT.

Do NOT build the UI.

Do NOT package.

Do NOT modify the existing TTS logic.

Do NOT install random dependencies.

Do NOT download massive models.

Do NOT create fake model implementations.

Do NOT delete anything.

Inspect:

scripts/tts_adapters.py
scripts/speech_synth_helper.py
voices/
configs/
requirements.txt
new .venv

Determine:

1. Required Python version
2. Required PyTorch version
3. Required TorchAudio version
4. Required Transformers version
5. Required CUDA runtime
6. GPU requirements
7. CPU requirements
8. Model-specific requirements
9. Voice requirements
10. External runtime requirements
11. Packaging requirements
12. Potential dependency conflicts

Then produce:

docs/INITIAL_RUNTIME_AUDIT.md

STOP.

Do not proceed to implementation until the audit is complete.


also crete a